#include <stdio.h>
#include <ctype.h>
int main() {
    char c;
    printf("Digite um caracter: ");
    scanf("%c", &c);
    if (isalpha(c)) {
        printf("1\n");
    } else {
        printf("0\n");
    }
    return 0;
}
