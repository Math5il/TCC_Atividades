#include <stdio.h>
#include <ctype.h>
int main() {
    char c;
    printf("Digite uma letra ou um dígito: ");
    scanf("%c", &c);
    if (isalnum(c)) {
        printf("1\n");
    } else {
        printf("0\n");
    }
    return 0;
}
